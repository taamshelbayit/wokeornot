<% layout('layout', { title: 'Users List' }) %>
<div class="card">
  <h2>Users</h2>
  <ul>
    <% users.forEach(function(user) { %>
      <li><%= user.username %> (<%= user.email %>)</li>
    <% }); %>
  </ul>
</div>
